import 'package:flutter/material.dart';

class MainFlutterBloc extends StatelessWidget {
  const MainFlutterBloc({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
